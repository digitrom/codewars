function sumStr(a,b) {
    let sum = +a + +b
    return sum.toString()
}
//  моя стрелочная
let = sumStr = (a,b) => ab = (+a + +b).toString();

let sumStr = (a,b) => String(+a + +b);

function sumStr(a,b) {
    return String(Number(a)+Number(b));
}

