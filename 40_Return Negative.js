function makeNegative(num) {
    if(num>0) {
        return -num
    }
    return num
}
//---------------------------------------------------
function makeNegative(num) {
    return num > 0 ? -num : num
}